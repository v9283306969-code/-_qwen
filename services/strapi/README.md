# Strapi CMS

Эта папка монтируется в контейнер Strapi при локальной разработке.

При первом запуске `docker-compose up` Strapi автоматически инициализирует проект в этой папке.

После инициализации здесь появятся файлы `package.json`, `config/`, `src/` и т.д.
